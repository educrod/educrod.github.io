---
layout: base.njk
---

<div class="posts">
  {% for post in collections.posts | reverse %}
    <article class="post">

      <h1><a href="{{ post.url }}">{{ post.data.title }}</a></h1>

      <div class="post-meta">
        {{ post.date | dateFormat }} :: {{ post.url | replace("/", "") }}
      </div>

      <div class="entry">
        {{ post.templateContent | excerpt | safe }}
      </div>

      <a href="{{ post.url }}" class="read-more">Read More</a>
    </article>
  {% endfor %}
</div>
